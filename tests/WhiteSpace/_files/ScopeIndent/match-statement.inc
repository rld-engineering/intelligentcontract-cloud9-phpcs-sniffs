<?php

declare(strict_types=1);

class Test
{
    
    private function dataChanges(Request $request): array
    {
        if ($changesJson) {
        }
        return array_map(
            fn (array $changeDetails) => match ($change) {
                ClassName::constantname => 1
            },
            $array
        );
    }
    
}
